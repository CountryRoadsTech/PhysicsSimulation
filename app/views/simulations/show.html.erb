<div class="mx-auto md:w-2/3 w-full flex">
  <div class="mx-auto">
    <h1 class="text-4xl font-semibold"><%= @simulation.name %></h1>

    <%= @simulation.description %>

    <table class="w-full table-auto border border-slate-300">
      <thead>
        <tr>
          <th class="w-1/3 border border-slate-300">
            Physics Bodies Count
          </th>
          <th class="w-1/3 border border-slate-300">
            Computed On
          </th>
          <th class="w-1/3 border border-slate-300">
            Computation Time (seconds)
          </th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td class="border border-slate-300">
            <%= @simulation.physics_bodies.size %>
          </td>
          <td class="border border-slate-300">
            <%= @simulation.computed_at.nil? ? 'Not Computed Yet' : @simulation.computed_at %>
          </td>
          <td class="border border-slate-300">
            <%= @simulation.computation_time.nil? ? '-' : @simulation.computation_time %>
          </td>
        </tr>
      </tbody>
    </table>

    <%= link_to 'Edit', edit_simulation_path(@simulation), class: "mt-2 rounded-lg py-3 px-5 bg-blue-600 inline-block text-white font-semibold" %>
    <div class="inline-block ml-2">
      <%= button_to 'Delete', simulation_path(@simulation), method: :delete, class: "mt-2 rounded-lg py-3 px-5 bg-red-600 text-white font-semibold" %>
    </div>
    <%= link_to 'Back', :back, class: "ml-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium" %>
    <br>
    <br>
    <hr>
    <ul class="list-disc m-4">
      <li><%= link_to 'About the Background Images', 'https://svs.gsfc.nasa.gov/4851', target: :_blank, class: 'text-blue-600 underline' %></li>
    </ul>

    <% if @simulation.computed? %>
      <canvas id="simulation_canvas" class="m-2 h-96 max-h-screen"></canvas>
    <% else %>
      <h3 class="text-2xl">Simulation not run yet. Once its been computed the visualization will appear here.</h3>
    <% end %>
  </div>
</div>
